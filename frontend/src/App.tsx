import { AppRouter } from './router'

/**
 * Main Application Component
 *
 * This component serves as the root of the application.
 * It wraps the entire app with the router configuration.
 */
function App() {
  return <AppRouter />
}

export default App
